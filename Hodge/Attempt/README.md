# Attempt sketches

Author: Benjamin Stanley Frohman (@BenFrohman).

These files name filtrations, type-shifts, and analytic slogans.
They are not `CycleSection` terms and they are not Mathlib PRs.

- `MonodromyTypeShift.lean` — `N(I^{p,q}) ⊆ I^{p-1,q-1}` when `W = W(N)`.
  Proof is `sorry`.

Do not import this folder from `Hodge.lean` until a file is actually a
theorem you want in the default build.
